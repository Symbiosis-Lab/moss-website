{
  "name": "moss-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:frontend": "vitest run main.test.js",
    "test:integration": "vitest run integration.test.js",
    "test:backend": "cd src-tauri && source $HOME/.cargo/env && cargo test",
    "test:all": "npm run test:frontend && npm run test:integration && npm run test:backend",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@tauri-apps/api": "2.7.0",
    "@tauri-apps/cli": "2.7.0",
    "@vitest/ui": "^3.2.4",
    "jsdom": "^26.1.0",
    "vite": "^5.4.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@tauri-apps/plugin-deep-link": "^2.4.1",
    "@tauri-apps/plugin-dialog": "^2.3.3"
  }
}
